# 前端团队研发协作与编码规范

## 1. 总则

为提升前端团队工程化水平，统一研发协作流程，规范代码提交质量，降低项目维护成本，特制定本规范。

本规范适用于所有前端项目，包括但不限于：

* PC 管理后台
* H5 活动页
* 小程序
* 中后台系统
* 内嵌 WebView 页面
* 临时营销活动页面
* 内部工具页面

所有前端开发人员必须严格遵守本规范。任何不符合本规范的代码，不得合入主分支，不得进入测试环境，不得参与正式发布。

---

## 2. 分支管理规范

### 2.1 分支命名规则

所有开发分支必须严格遵守以下格式：

```text
feature/模块名-需求编号-开发人姓名拼音
bugfix/模块名-缺陷编号-开发人姓名拼音
hotfix/模块名-线上问题编号-开发人姓名拼音
refactor/模块名-重构内容-开发人姓名拼音
```

示例：

```text
feature/user-JIRA-1024-zhangsan
bugfix/order-BUG-2098-lisi
hotfix/pay-ONLINE-3321-wangwu
```

禁止使用以下分支名称：

```text
dev
test
fix
tmp
aaa
my-branch
feature/test
bugfix/fix
```

不符合命名规范的分支，不允许推送远程仓库。

---

## 3. Commit Message 规范

### 3.1 提交格式

所有 Commit Message 必须严格遵守以下格式：

```text
type(scope): [JIRA编号] 提交说明
```

其中：

```text
type 必须为 feat、fix、docs、style、refactor、perf、test、build、ci、chore、revert 之一
scope 必须为具体业务模块名称
JIRA编号必须真实存在
提交说明必须使用中文
提交说明不得少于 8 个汉字
```

正确示例：

```text
feat(user): [JIRA-1024] 新增用户详情编辑功能
fix(order): [BUG-2098] 修复订单金额展示异常问题
```

错误示例：

```text
fix
update
改一下
fix bug
feat: 新增功能
fix(order): 修复问题
fix(order): [JIRA-1] 修复
```

以上提交均不允许通过。

### 3.2 禁止跳过提交校验

严禁使用以下命令绕过提交规范：

```bash
git commit --no-verify
git push --no-verify
HUSKY=0 git commit
```

一经发现，需在团队群内说明原因，并补充规范提交记录。

---

## 4. TypeScript 类型规范

### 4.1 严禁使用 any

业务代码、公共组件、工具函数、接口定义、状态管理中，均严禁出现 `any`。

包括但不限于：

```ts
any
any[]
Record<string, any>
Promise<any>
Function
Object
```

错误示例：

```ts
const data: any = {};
```

正确示例：

```ts
interface UserInfo {
  id: number;
  name: string;
}
```

### 4.2 严禁使用 unknown 规避 any

`unknown` 仅允许在底层工具函数中使用，业务代码中不得使用 `unknown` 作为后端数据兜底类型。

错误示例：

```ts
type MagicType = unknown;

interface UserRes {
  dynamicExtData: MagicType;
}
```

正确示例：

```ts
interface UserRes {
  dynamicExtData: {
    id: number;
    name: string;
    tags: string[];
    isVip: boolean;
  };
}
```

### 4.3 接口出入参必须完整定义

所有后端接口必须定义请求参数和响应参数。

```ts
interface GetUserReq {
  userId: number;
}

interface GetUserRes {
  userId: number;
  userName: string;
  age: number;
  avatar: string;
  phone: string;
  email: string;
  address: string;
  createTime: string;
  updateTime: string;
}
```

禁止接口响应直接使用内联类型：

```ts
http.get<{ name: string }>('/api/user');
```

必须单独定义 Interface：

```ts
interface UserRes {
  name: string;
}

http.get<UserRes>('/api/user');
```

---

## 5. 空值处理规范

### 5.1 所有嵌套字段必须使用可选链

读取后端返回的嵌套字段时，必须使用可选链。

错误示例：

```ts
const name = data.user.info.name;
```

正确示例：

```ts
const name = data?.user?.info?.name;
```

### 5.2 所有展示字段必须设置默认值

页面展示字段不得直接使用后端返回值，必须提供默认值。

错误示例：

```tsx
<div>{user.name}</div>
```

正确示例：

```tsx
<div>{user?.name || '-'}</div>
```

### 5.3 默认值必须符合字段类型

字符串默认值统一使用：

```ts
'-'
```

数字默认值统一使用：

```ts
0
```

数组默认值统一使用：

```ts
[]
```

对象默认值统一使用：

```ts
{}
```

布尔值默认值统一使用：

```ts
false
```

错误示例：

```ts
const name = user?.name || '';
const list = user?.tags || null;
```

正确示例：

```ts
const name = user?.name || '-';
const list = user?.tags || [];
```

---

## 6. 网络请求规范

### 6.1 所有请求必须封装为 API 方法

组件中不得直接调用请求实例。

错误示例：

```ts
http.get('/api/user');
```

正确示例：

```ts
export function getUserInfo(params: GetUserReq) {
  return http.get<GetUserRes>('/api/user', params);
}
```

组件中只能调用 API 方法：

```ts
getUserInfo({ userId });
```

### 6.2 API 文件命名规范

所有接口文件必须放置在：

```text
src/api
```

命名格式：

```text
模块名.api.ts
```

示例：

```text
user.api.ts
order.api.ts
payment.api.ts
```

禁止出现：

```text
request.ts
service.ts
api.ts
test.ts
接口.ts
```

---

## 7. React 组件规范

### 7.1 组件必须使用函数声明

禁止使用箭头函数声明组件。

错误示例：

```tsx
const UserCard = () => {
  return <div />;
};
```

正确示例：

```tsx
function UserCard() {
  return <div />;
}
```

### 7.2 组件 Props 必须以组件名 + Props 命名

错误示例：

```ts
interface Props {
  name: string;
}
```

正确示例：

```ts
interface UserCardProps {
  name: string;
}
```

### 7.3 单个组件文件不得超过 300 行

任何组件文件超过 300 行，必须拆分组件。

超过 300 行的代码不允许合入。

### 7.4 JSX 嵌套不得超过 4 层

JSX 嵌套层级超过 4 层，必须拆分子组件。

错误示例：

```tsx
<div>
  <div>
    <div>
      <div>
        <span>text</span>
      </div>
    </div>
  </div>
</div>
```

---

## 8. Hooks 使用规范

### 8.1 useEffect 必须写清楚依赖

禁止出现空依赖数组，除非添加注释说明原因。

错误示例：

```ts
useEffect(() => {
  getUserInfo();
}, []);
```

正确示例：

```ts
useEffect(() => {
  getUserInfo();
}, [userId]);
```

如果确实只需要执行一次，必须写注释：

```ts
useEffect(() => {
  getUserInfo();
  // 仅页面初始化时请求一次
}, []);
```

### 8.2 useEffect 内禁止直接写复杂逻辑

错误示例：

```ts
useEffect(() => {
  const a = data.map(item => item.name).filter(Boolean).join(',');
  setName(a);
}, [data]);
```

正确示例：

```ts
function formatName(data: UserInfo[]) {
  return data.map(item => item.name).filter(Boolean).join(',');
}

useEffect(() => {
  setName(formatName(data));
}, [data]);
```

---

## 9. 样式规范

### 9.1 className 必须语义化

禁止使用无意义 className。

错误示例：

```tsx
<div className="box">
<div className="content">
<div className="left">
```

正确示例：

```tsx
<div className="user-card">
<div className="user-card__content">
<div className="user-card__avatar">
```

### 9.2 禁止内联样式

错误示例：

```tsx
<div style={{ color: 'red' }} />
```

正确示例：

```tsx
<div className="error-text" />
```

### 9.3 颜色值必须使用变量

错误示例：

```css
color: #ff0000;
```

正确示例：

```css
color: var(--color-error);
```

---

## 10. 代码格式规范

### 10.1 禁止魔法数字

错误示例：

```ts
if (status === 1) {
  showSuccess();
}
```

正确示例：

```ts
const STATUS_SUCCESS = 1;

if (status === STATUS_SUCCESS) {
  showSuccess();
}
```

### 10.2 禁止函数超过 50 行

任何函数超过 50 行必须拆分。

### 10.3 禁止出现 console

除本地调试外，代码中不得出现：

```ts
console.log
console.warn
console.error
```

提交前必须删除。

### 10.4 禁止出现 TODO

禁止将未完成逻辑以 TODO 形式提交到主分支。

错误示例：

```ts
// TODO: 后续优化
```

---

## 11. Code Review 规范

### 11.1 所有 MR 必须至少 2 人 Review

每个 Merge Request 必须至少获得 2 名团队成员 Approve，方可合入。

其中至少 1 人必须为前端组长或模块 Owner。

### 11.2 Review 评论必须全部解决

所有 Review 评论必须被 Resolve。

未 Resolve 的 MR 不允许合入。

### 11.3 禁止口头 Review

所有 Review 意见必须写在 MR 评论中，不得仅通过私聊、语音或会议口头确认。

---

## 12. 发版规范

### 12.1 发版前必须完成自测清单

每个需求发版前必须填写自测清单，包括：

```text
页面是否正常打开
接口是否正常返回
空数据是否正常展示
异常数据是否正常展示
按钮是否防重复点击
接口失败是否有提示
移动端是否适配
权限是否校验
埋点是否验证
```

未填写自测清单不得提测。

### 12.2 禁止临时修改后直接发版

发版前 2 小时内原则上禁止合入新代码。

如需合入，必须由前端组长、测试负责人、产品负责人三方确认。

---

## 13. ESLint 与 Husky 拦截规则

项目必须开启以下检查：

```text
TypeScript 类型检查
ESLint 规则检查
Prettier 格式检查
Commit Message 检查
分支命名检查
console 检查
any 检查
TODO 检查
文件行数检查
函数行数检查
JSX 嵌套层级检查
```

任何检查不通过，均禁止提交代码。

---

## 14. 违规处理

若发现违反本规范的代码：

第一次：MR 中提醒并要求修改；
第二次：团队群内通报；
第三次：暂停合入权限，由前端组长进行代码规范培训。

如因绕过规范导致线上事故，需补充事故复盘，并在复盘中说明：

```text
违反了哪条规范
为什么没有遵守
为什么 Review 没有发现
后续如何避免
```
